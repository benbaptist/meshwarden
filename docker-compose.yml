services:
  meshwarden:
    build:
      context: ./backend
    ports:
      - "${HOST_PORT:-5001}:${PORT:-5000}"
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
    devices: []  # Add serial devices here, e.g. /dev/ttyUSB0:/dev/ttyUSB0
