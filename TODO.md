- Keeps reverting to login page when I access `/`
- if I am at `/` and I reload once, it shows the same page without the left sidebar. reload again and it goes back to login screen, so a bit of a tie to the bug above. 

- Repeaters show up in the Chats menu, despite MeshCore not typically allowing conversation with repeaters. Should be removed

- Contacts page improvements
    - Change sorting by clicking on various columns
    - shorthand for actions on wide-enough viewports